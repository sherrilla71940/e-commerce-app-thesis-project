import express from "express";
// import checkoutCart from "../controllers/checkout-controller"
//import controllers

export default function checkoutRouter(router: express.Router) {
  //  router.post("/checkout/:cartId", checkoutShoppingCart);
}
